{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "title": "Tasks.Pricing.PriceSet.v1",
  "description": "json schema for tasks price update events (version 1)",

  "definitions": {
    "event_data": {
      "type": "object",
      "properties": {
        "public_id": {
          "type": "string"
        },
        "price": {
          "type": "string"
        },
        "fee": {
          "type": "string"
        }
      },
      "required": [
        "public_id",
        "price",
        "fee"
      ]
    }
  },

  "type": "object",

  "properties": {
    "event_id":      { "type": "string" },
    "event_version": { "enum": [1] },
    "event_name":    { "enum": ["TaskPriceSet"] },
    "event_time":    { "type": "string" },
    "producer":      { "type": "string" },

    "data": { "$ref": "#/definitions/event_data" }
  },

  "required": [
    "event_id",
    "event_version",
    "event_name",
    "event_time",
    "producer",
    "data"
  ]
}

